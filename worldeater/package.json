{
  "name": "worldeater",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/server && cp src/worker.js dist/server/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.182.0"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vitest": "^2.1.5"
  }
}
