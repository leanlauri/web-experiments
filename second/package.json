{
  "name": "second",
  "private": true,
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build",
    "validate": "npm run typecheck && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.8.0",
    "@types/bun": "latest",
    "eslint": "^9.8.0",
    "globals": "^15.9.0",
    "typescript": "^5.6.3",
    "vite": "^7.3.1",
    "vitest": "^2.1.5"
  }
}
